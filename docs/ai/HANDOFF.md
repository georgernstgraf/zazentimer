# Hand Off

## Open Tasks
1. [ ] #255: Run `scripts/run-instrumentation.sh` to verify the new `am instrument` two-phase approach works on all API levels (or physical device).
2. [ ] #255: Create `BackupRestoreUiTest.kt` (UiAutomator, SAF picker flow) — future task, not blocking for #255 closure.

Last updated: 2026-06-13