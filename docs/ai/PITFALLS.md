- Android 14+ requires `FOREGROUND_SERVICE` and explicit types (e.g. `FOREGROUND_SERVICE_SPECIAL_USE`) when starting Foreground Services. Use `ServiceInfo.FOREGROUND_SERVICE_TYPE_SPECIAL_USE` in `startForeground()`.
- `AlarmManager.setExactAndAllowWhileIdle()` is NOT sufficient for reliable background timing on all devices — it can still be suppressed by aggressive battery optimization. Use `setAlarmClock()` for timer/alarm events that must fire at an exact time.
- `setAlarmClock()` requires RTC (wall clock) time, not `ELAPSED_REALTIME_WAKEUP`. When switching, also update elapsed-time calculations from `SystemClock.elapsedRealtime()` to `System.currentTimeMillis()`.
- BroadcastReceivers registered at runtime (`registerReceiver()`) die with the process. For alarms that must survive process death, register the receiver statically in `AndroidManifest.xml`.
- On many OEM Android devices (Oppo/OnePlus/Realme with OplusHansManager, Android 11+), implicit broadcast PendingIntents are blocked with "Background execution not allowed" even when sent via `AlarmManager.setAlarmClock()`. Always use **explicit Intents** (`.setClass(context, ReceiverClass.class)`) in alarm PendingIntents to guarantee delivery.
- **Stale build artifacts cause phantom test failures.** When resource files are deleted or renamed (layouts, strings, IDs), incremental builds can leave outdated R.class/R.txt entries that cause instrumented tests to reference wrong resource IDs. The tests appear to fail with `NoMatchingViewException` even though the code is correct. **Always run `gradle clean` before building after deleting or renaming resource files.** If instrumented tests fail after a resource change, clean build before investigating.
- **Always check GitHub Actions after pushing.** Use `gh run list --limit 3` to verify CI passes. A broken CI pipeline can go unnoticed for many commits if not explicitly checked.
- **Hilt instrumented tests require `HiltTestRunner`** as the test instrumentation runner AND `androidTestAnnotationProcessor 'com.google.dagger:hilt-compiler:...'` in `build.gradle`. Without both, tests fail at runtime with `IllegalStateException` or missing generated component files.
- **Java test class names must exactly match their file names.** A file named `BackupRestoreTest.java` must declare `public class BackupRestoreTest`, not `ZazenTimerActivityTest`. Mismatches cause compilation failures only caught on CI (local `./gradlew build` may not compile androidTest sources).
- **Room schema must exactly match the legacy database schema.** When migrating from raw SQLiteOpenHelper to Room, `PRAGMA table_info()` differences (type case like `integer` vs `INTEGER`, nullable vs NOT NULL) cause `IllegalStateException: Migration didn't properly handle`. Always test migrations on a physical device with real user data — the emulator starts with a fresh DB.
- **JUnit `@Rule` ordering is required with Hilt + ActivityScenarioRule.** Without `@Rule(order = 0)` on `HiltAndroidRule` and `@Rule(order = 1)` on `ActivityScenarioRule`, the activity may launch before Hilt injects dependencies. This is device-dependent (fails on API 31, may pass on API 35). `hiltRule.inject()` takes no arguments (not `inject(this)`).
- **Multiple ADB devices cause UTP test runner NullPointerException.** `./gradlew connectedDebugAndroidTest` fails with 0 tests and a NullPointerException when 2 devices are connected. Disconnect one device first, or use `adb shell am instrument` directly on a single device.
- **`./gradlew build` does NOT compile androidTest sources.** Run `./gradlew assembleDebugAndroidTest` separately to catch test compilation errors locally before pushing to CI.
- **`adb logcat --pid=PID` loses tracking when app restarts.** If the app crashes and relaunches, the PID changes and logcat stops capturing. Use tag-based or app-based filtering instead: `adb logcat at.priv.graf.zazentimer:V *:S`. For full correlation with screen navigation, use unfiltered `adb logcat` piped to a file.
- **`uiautomator dump` can return null root node immediately after app launch.** Wait 2–3 seconds after `am start` before dumping. Also, tapping popup menu items via coordinates is fragile — always verify the popup items from the dump before tapping.
- **`DbOperations.duplicateSession()` was crashing** with `SQLiteConstraintException: UNIQUE constraint failed: sessions._id`. The method copied the session including its primary key and tried to insert with `@Insert` which conflicted. **Fixed in #52**: set `source.id = 0` before inserting (Room auto-generates). Same pattern already existed for sections. Lesson: when duplicating entities with `@PrimaryKey(autoGenerate = true)`, always reset the PK to 0 before `@Insert`.
- **Espresso `AmbiguousViewMatcherException` with RecyclerView child views.** When a RecyclerView has multiple items, each containing a child view with the same ID (e.g., `sessionOverflow`), `onView(withId(R.id.sessionOverflow))` matches all of them. Use `RecyclerViewActions.actionOnItemAtPosition(pos, clickChildViewWithId(childId))` with a custom `ViewAction` that calls `view.findViewById(id).performClick()` to target a specific item's child view.
- **Gradle `android.injected.signing.store.file` resolves relative to the daemon working directory**, not the project directory. On CI, this means a relative path like `release.keystore` resolves to `~/.gradle/daemon/7.5/release.keystore`. Always use an absolute path (e.g., `$RUNNER_TEMP/release.keystore`).
- **GitHub Actions YAML multi-line `run:` with backslash continuations** can break if indentation is wrong — each line may be treated as a separate command. For complex Gradle commands with many `-P` flags, use a single line or a shell script step.
- **LiveData in activity-scoped ViewModels retains stale values across fragment recreations.** When a fragment is destroyed (e.g., via tab navigation) and later recreated, its observer re-subscribes to the same LiveData instance and immediately receives the last emitted value — even if that value is now stale (e.g., `running=true` after the service has stopped). Always clear LiveData when the underlying state becomes invalid (e.g., set to null in `stopUpdateThread()`).
- **`showIdleState()` must reset ALL idle-related fields, not just UI visuals.** If a method is called `showIdleState()`, it must set the `meditationRunning` flag to `false` — otherwise the UI looks correct but the click handler is routed to the wrong code path.
- **`./gradlew connectedDebugAndroidTest` may fail with protobuf classloader conflicts** on some Gradle versions, even though the tests pass fine via `adb install` + `adb shell am instrument`. If Gradle's UTP runner fails, fall back to manual APK install + `am instrument`.
- **`values/bools.xml` and `values/integers.xml` must NOT be deleted.** They contain AppCompat `abc_*` overrides (`abc_config_closeDialogWhenTouchOutside`, `abc_config_activityDefaultDur`, etc.) that are referenced by the app's theme/styles. Removing them causes `AAPT: error: resource bool/... not found`. They may look like unused library copies, but they are required.
- **`MeditationService.isRunning` must be set to `false` in `onMeditationEnd()`, not just `onDestroy()`.** `stopSelf()` is asynchronous — `onDestroy()` runs later. Any code checking `isServiceRunning()` immediately after the end broadcast (e.g., `MainFragment.onResume()`) will see a stale `true` and leave the UI in a disabled state. Setting the flag in `onMeditationEnd()` makes it immediately visible.
- **`unmutePhone()` must not blindly restore ringer state.** Users may manually change the ringer mode during meditation (via hardware keys or settings). Record `mutedRingerMode` when muting, compare to current mode before restoring — skip if they differ.
- **`DuplicateSessionTest` fails with `AmbiguousViewMatcherException`** if the database is not reset between runs. Multiple "Copy of" sessions accumulate from previous executions. **Fixed by calling `resetDatabaseForTest()` in `@Before`**.
- **Dim Bell slider is inverted from stored `section.volume`.** SeekBar progress 0 = no dimming (volume=100), progress 90 = max dimming (volume=10). The mapping `volume = 100 - progress` must be maintained in both `fillViewFromData()` and `fillDataFromViews()`. Max progress is 90 (not 100) because 100% dimming is considered useless.
- **Legacy `section.volume` values below 10 must be clamped** in `fillViewFromData()`. Old data may have volume=0, which maps to dimProgress=100, exceeding the SeekBar max of 90. Always clamp to `[0, 90]` and snap to multiples of 10.
- **MainFragment session card tap selects, does NOT navigate.** Tapping a session card only highlights it and saves the selection to preferences. To reach `SessionEditFragment`, tap the three-dot overflow menu on the card and choose "Edit Session" — or create a new session via FAB which auto-navigates to edit.
- **`uiautomator dump` default path not writable on API 29 emulator.** The default path `/sdcard/window_dump.xml` is not writable. Use an explicit path: `adb shell uiautomator dump /data/local/tmp/window_dump.xml` then `adb pull`.
- **Emulator API 29 cold boot shows ANR dialog.** "System UI isn't responding" dialog appears during cold boot and blocks `input tap` commands. Dismiss it by tapping "Wait" before interacting.
- **Emulator StorageManager race after boot.** `adb install` fails with `NullPointerException: StorageManager.getVolumes() returns null` shortly after `sys.boot_completed=1`. Wait until `adb shell pm list packages` returns results (~10s after boot_completed) or kill and restart the emulator.
- **`pm` service not ready at `sys.boot_completed=1`.** On API 29 emulator, `adb shell pm install` returns "Can't find service: package" for ~40s after cold boot even though boot_completed=1. Wait for `pm list packages` to succeed before installing.
- **Emulator reboot needs ~45s for stable state.** After `adb reboot`, `sys.boot_completed=1` at ~30s but system services remain unstable. Add 15s additional wait before interacting.
- **Batch translations via deep_translator can time out.** 12 languages × 141 strings = ~1700 API calls. With `sleep(0.05)` per call, this takes ~7-10 minutes. Use a large timeout (≥20 minutes) and consider splitting runs or reducing sleep to 0.01s. Non-Latin scripts (Odia, Pashto) are especially vulnerable to format specifier corruption — always verify with `grep -c '⦅'` after translation to confirm no leftover placeholders.
- **googletrans vs deep-translator.** `googletrans==4.0.0-rc1` returns empty JSON for many languages — don't use it. Use `deep-translator` (GoogleTranslator) instead: `pip install deep-translator`.
- **Format specifier corruption by Google Translate.** Non-Latin scripts (Amharic, Georgian, Burmese, Khmer, Thai, Arabic, Hindi) corrupt `%1$d` and `%1$s` by converting Latin characters to local-script equivalents (`%1$d` → `%1$д` in Cyrillic). Mask ALL format specifiers with ASCII placeholder tokens (`__FMT0__`, `__NL__`) before sending to Google Translate. Unicode bracket placeholders (`⦅0⦆`) are themselves corrupted in some scripts.
- **`\n` corruption in segment translation.** When translating `help_sectionlist_text`, split by `\n`, translate each segment individually, then rejoin. Batch/single-pass translation corrupts `\n` sequences in RTL and non-Latin scripts.
- **`character_counter_pattern` must never be translated.** `%1$d / %2$d` is a pure format pattern — Google Translate converts digits to local numerals even in Latin-script languages.
- **AAPT2 rejects raw apostrophes in string values.** Replace `'` → `\'` in output XML. Use regex `(?<!\\\\)'` to skip already-escaped `\'`.
- **Short strings may return untranslated.** Google Translate sometimes returns the source unchanged for very short strings (e.g., "OK", "Help", section names). This is acceptable.
- **`ContextCompat.getDrawable()` has no 3-arg overload** in `androidx.core:core:1.9.0`. The method signature `getDrawable(ctx, id, theme)` does not exist. Use `ResourcesCompat.getDrawable(getResources(), id, getTheme())` instead when you need theme-aware drawable loading.
- **`-Xlint:deprecation` must go in `app/build.gradle`**, not `gradle.properties` under `org.gradle.jvmargs`. The JVM args only affect the Gradle daemon process, not the javac compiler options. Use `tasks.withType(JavaCompile).configureEach { options.compilerArgs << "-Xlint:deprecation" }` to surface hidden deprecation warnings during compilation.
- **Gradle UTP runner fails on API 35+** — `./gradlew connectedDebugAndroidTest` discovers 0 tests on API 35/36 emulators regardless of execution mode (orchestrator or host). The Gradle Unified Test Platform (UTP) that manages test execution fails silently while `adb shell am instrument -w` runs all tests successfully. **Fix:** Use direct `am instrument` for API 35+ test jobs. Requires `target: google_apis` on the emulator system image.
- **Espresso `RootViewWithoutFocusException` on API 35** — all Espresso tests fail with `has-window-focus=false` if the emulator screen is asleep or keyguard is active. Must run `adb shell svc power stayon true` + `adb shell input keyevent KEYCODE_WAKEUP` before running tests on API 35. This was not needed on API 29.
- **`avdmanager create avd` can fail with "Package path is not valid. Valid system image paths are: null"** due to SDK XML version mismatches between cmdline-tools and installed system images. Workaround: create the AVD directory and config files manually by copying from an existing AVD (e.g., API 35) and changing `image.sysdir.1`, `target`, and `tag.*` fields to match the new API level.
- **Enabling `-Xlint:deprecation` surfaces previously hidden warnings.** After fixing the initial set of deprecations (the ones the compiler warned about without the flag), enabling the lint flag revealed 5+ additional warnings in files that were previously silent (e.g., `getAdapterPosition()`, `setHasOptionsMenu()`, `SCREEN_BRIGHT_WAKE_LOCK`). Always do a clean build after enabling the flag to catch the full set.
- **Espresso idle check fails while foreground service polls with `Handler.postDelayed`.** The meditation service posts frame callbacks every ~300ms for timer UI updates. Espresso waits for the main looper to idle before any action/assertion, but the `Choreographer$FrameDisplayEventReceiver` keeps the looper busy indefinitely. **Fix:** Use **UI Automator** instead of Espresso for interactions during active meditation. UI Automator doesn't wait for the main looper to idle. See `MeditationServiceTest.java` for the implementation pattern.
- **Meditation tests need `forceStopMeditationForTest()` in `@After`.** The `MeditationViewModel`'s update thread (`Handler.postDelayed` 300ms loop) keeps the main looper busy between tests, causing `AppNotIdleException` on the second test. Calling `activity.forceStopMeditationForTest()` in `@After` stops the handler, allowing Espresso to proceed.
- **UI Automator dialog button clicks must use `className("android.widget.Button")`.** A `UiSelector.text("Stop")` matches the meditation screen's stop button, not the dialog's "Stop" button. Use `textContains("Stop").className("android.widget.Button")` to target dialog buttons specifically.
- **`PreferenceFragmentCompat` scrolling in Espresso is unreliable.** `scrollTo()` with `withText()` may fail to find off-screen preferences. Use `RecyclerViewActions.scrollToPosition(99)` on `android.R.id.list` as a fallback, but even this may not work on CI headless emulators. The `SettingsPage.clickBackup()`/`clickRestoreAndConfirm()` tests are still flaky on CI due to this.
- **`translate_batch9.py` dropped excluded strings from locale files.** The old script's `build_strings_xml()` only wrote strings present in its result dict. Excluded (keep-english) strings were included, but only for the batch's languages — 31 locales from other batches never got `about1`, `about2`, `about3`, `app_name`, etc. Fixed by `retranslate.py --diff` which always copies keep-english strings from source.
- **`retranslate.py` must decode XML entities when reading, then re-encode when writing.** Source strings like `&lt;a href=...&gt;` are stored as XML entities in `strings.xml`. The script's `extract_strings()` must decode `&lt;` → `<` and `&amp;` → `&`, then `xml_escape()` re-encodes them on output. Without decoding, double-escaping occurs (`&amp;lt;`).
- **Xvfb: do NOT use `-no-window` with virtual display.** When running instrumented tests on VPS with Xvfb, the emulator must run WITHOUT the `-no-window` flag. Use `-gpu swiftshader_indirect` only. With `-no-window`, the emulator has no display surface, defeating the purpose of Xvfb. The `$DISPLAY` env variable must be set (e.g., `DISPLAY=:99`) and Xvfb must be running before launching the emulator.
- **API 35+ UTP bug: `./gradlew connectedDebugAndroidTest` finds 0 tests.** Must use `am instrument` directly on API 35+ (Gradle UTP bug). Build APKs first with `assembleDebug assembleDebugAndroidTest`, then `adb install -r` both APKs, then `adb shell am instrument -w at.priv.graf.zazentimer.test/at.priv.graf.zazentimer.HiltTestRunner`.
- **`ANDROID_HOME` not set on VPS.** Scripts must explicitly set `export ANDROID_HOME=/opt/android-sdk` and add emulator/platform-tools to PATH before invoking Gradle or emulator commands.
- **`Html.fromHtml()` collapses `\n` to whitespace.** When switching from `setMessage()` (plain text) to `setView()` with `Html.fromHtml()` for clickable links, all `\n` characters are treated as HTML whitespace and rendered as single spaces. Replace `\n` with `<br>` (or `\n\n` with `<br><br>`) in the Java string and use `&lt;br&gt;` in XML string resources.
- **Popup/toolbar overflow menu items must use `withText()` not `withId()`.** Menu items are rendered inside `MenuPopupWindow$MenuDropDownListView` (an `AdapterView`), and `onView(withId(...))` cannot find them in the view hierarchy. Use `onView(withText(R.string.menu_xxx))` instead. Also add `SystemClock.sleep(500)` after opening the overflow and before clicking the item.
- **`View.post()` does NOT wait for layout to complete.** It enqueues a Runnable after the current Looper message. During fragment transitions (MaterialFadeThrough), `onResume()` fires before layout completes, and `post()` executes with pre-layout measurements. `RecyclerView.computeVerticalScrollRange()` returns 0 or stale values in this window, causing height-capping logic to skip (condition `0 > cap` is false).
- **One-shot `OnGlobalLayoutListener` fires with transient parent heights.** During fragment back-navigation, the parent RelativeLayout may report a smaller height (e.g., 1205px vs 2038px) while the keyboard is dismissing or the transition is in progress. If the listener removes itself after computing a cap from this transient height, subsequent parent height changes are silently ignored — resulting in a stale cap (e.g., 588 instead of 1087, giving 67% of space to the image instead of 40%). Use a **persistent** listener (never remove via `removeOnGlobalLayoutListener`) with a value-change guard.
- **`onCreateView` resets `maxHeightPx` to `Integer.MAX_VALUE`.** On view recreation (Navigation Component back-pop), the custom RecyclerView starts uncapped. The listener fires asynchronously — the first `onMeasure()` pass is uncapped. A persistent listener corrects this on the next layout pass; the transient wrong frame is hidden by the fragment transition animation.
- **Navigation Component back-pop recreates fragment views.** With `setReorderingAllowed(true)` (default in Navigation 2.7+), navigating away destroys the fragment's view (`onDestroyView`) and returning recreates it (`onCreateView`). Any state stored in view fields is lost and must be re-initialized in `onCreateView()`.
- **Keyboard dismiss during back-navigation causes multiple parent height changes.** When popping from a fragment where the keyboard was open (e.g., editing text), the parent undergoes several layout passes as the keyboard animates away. Any height-dependent computation must handle this transient state gracefully.
- **`TextUtils.isEmpty()` does not trim.** It checks null and zero-length but NOT whitespace-only strings. When hiding a TextView based on empty content, use `text.trim().isEmpty()` instead to also catch `"   "` — otherwise invisible whitespace still reserves vertical space.
- **Vertical LinearLayout children sit at top, not centered.** In a horizontal LinearLayout with `center_vertical` siblings, a vertical LinearLayout child will align its content to its own top edge — causing misalignment when it's shorter than siblings. Add `android:layout_gravity="center_vertical"` to the vertical LinearLayout itself.
- **Orchestrator signature mismatch after API level switch.** When switching between emulators with different API levels, the test orchestrator APK may have a different signature than the one cached on the device. Uninstall with `adb uninstall androidx.test.orchestrator` and `adb uninstall androidx.test.services` before running tests, or use `--no-directory-stats` to force reinstall.
- **`sdkmanager` can silently produce empty system images.** If the download is interrupted (timeout, network issue), the system image directory is created but contains only a `.installer` subdirectory — no kernel, no system.img. Always verify with `ls system-images/android-XX/tag/x86_64/kernel-ranchu` after installation. Delete and reinstall if missing.
- **`nohup` emulator startup is required for automated test scripts.** Starting the emulator in the foreground within a shell script causes the script to block on the emulator process. Use `nohup emulator ... &` and wait for `adb wait-for-device` + `sys.boot_completed=1` in a separate loop.
- **New AVD cold boots take ~100s, subsequent boots ~45s.** Plan 2 minutes wait for first boot of a new AVD. Use `sleep 15` after `boot_completed=1` for system service stability (PITFALLS #34).
- **`run-nightly.sh` does `git reset --hard origin/main` + `git clean -fdx`.** It destroys all uncommitted local changes. Never run it while you have uncommitted work. It also deletes `local.properties` — scripts must recreate it before building.
- **`ANDROID_SDK_ROOT` must be set alongside `ANDROID_HOME`.** The emulator binary requires `ANDROID_SDK_ROOT` to locate system images. Without it, some AVDs fail with "Broken AVD system path" even though `ANDROID_HOME` is set. Both `run-nightly.sh` and `run-stage2.sh` now export it.
- **AVD `image.sysdir.1` paths can be wrong.** Some AVDs had `android-sdk/system-images/...` (relative to SDK root but doubled) instead of `system-images/...` (correct relative path) or absolute `/opt/android-sdk/system-images/...`. Check all AVD configs with `grep image.sysdir.1 ~/.android/avd/test_api*/avd/config.ini` if emulators fail to start.
- **Emulator `-target google_apis` flag removed in emulator 36.5.10.** The flag causes "unknown option" error and the emulator never starts. The tag is already in the AVD `config.ini` (`tag.id=google_apis`). Don't pass `-target` on the command line.
- **`RootViewWithoutFocusException` is intermittent on Xvfb.** The `has-window-focus=false` error affects random API levels per run — not consistent across runs. Not a code issue. `svc power stayon` + `KEYCODE_WAKEUP` + `KEYCODE_HOME` help but don't eliminate it. Tests that DO get focus all pass, proving code correctness.
- **Bash tool timeout kills background processes.** Long-running commands (emulators, test scripts) get terminated by the bash tool's timeout. Use `nohup script.sh > /tmp/log 2>&1 &` to detach, then monitor with `tail /tmp/log`. Never run emulator-dependent workflows inline in a bash call.
- **`Notification.flags |= 98` is `FLAG_ONGOING_EVENT(2) | FLAG_NO_CLEAR(96)`.** Use `NotificationCompat.Builder.setOngoing(true)` instead of setting raw flag bits.
- **Room `allowMainThreadQueries()` replacement: `ExecutorService` + `submit().get()`.** Wrapping DAO calls in `executor.submit(callable).get()` runs queries on a background thread (Room is happy) while blocking the caller (API surface unchanged). Write operations use `executor.execute()` (fire-and-forget). No caller changes needed. The coroutines migration (#106) will make reads truly non-blocking later.
- **Never run ad-hoc emulator/test commands.** Always use the existing scripts (`scripts/run-stage2.sh`, `scripts/run-nightly.sh`) which handle SDK path detection, AVD resolution, Xvfb/DISPLAY detection, emulator lifecycle, and fail-fast logic. Ad-hoc commands miss environment setup, wrong SDK paths, missing AVDs, and incorrect flags.
- **DbOperations executeSync deadlock.** With a single-thread ExecutorService, `executeSync()` wraps a callable in `executor.submit().get()`. If the callable calls another method that also uses `executeSync()`, it deadlocks because the single thread is blocked on `.get()`. Composite methods like `duplicateSession()` must access DAOs directly inside their `executeSync` block.
- **AndroidX PreferenceFragmentCompat uses RecyclerView (id=recycler_view), not ListView (id=android.R.id.list).** Tests that reference `android.R.id.list` will fail with NoMatchingViewException. Use `R.id.recycler_view` instead.
- **SystemClock.sleep() is still required for popup menus, fragment transitions, and PreferenceFragment loading.** Espresso's idle mechanism does not track popup menu animations, Material Motion fragment transitions, or PreferenceFragment RecyclerView initialization. #109's removal of all sleeps was overzealous.
- **Gradle UTP causes RootViewWithoutFocusException on API 33/34.** Use `am instrument` directly for API 33+ instead of `./gradlew connectedDebugAndroidTest`. Add a retry mechanism for focus errors.
- **`am instrument` exit code is 0 even when tests fail.** Parse output for `Failures: N` to detect test failures.
- **`am instrument` without orchestrator shares state between tests.** Each test class must call `resetDatabaseForTest()` in `@Before` if it modifies database state. `SessionCrudTest` was missing this call.
- **Soft keyboard consumes pressBack().** After typing text, `Espresso.pressBack()` dismisses the keyboard instead of navigating back. Call `closeSoftKeyboard()` before `pressBack()`.
- **`set -e` + `grep -c` kills scripts.** `grep -c` returns exit code 1 when no matches are found, which triggers `set -e` and terminates the script. Always use `grep -c ... || true` in scripts with `set -e`.
- **`setsid` for process detachment from bash tool.** `nohup` does not survive the bash tool's timeout — the process tree gets killed when the tool times out. Use `setsid` to create a new session and fully detach from the parent process tree.
- **`clean_device_packages()` must only remove old-namespace packages.** Only uninstall `de.gaffga.android.zazentimer` (legacy namespace). Do not remove current-namespace (`at.priv.graf.zazentimer`) or test framework packages (`androidx.test.orchestrator`, `androidx.test.services`).
- **Kotlin: `data class` with `var` fields requires caution with `copy()`.** When BO classes use `var` for Room entities, `copy()` creates a new instance that Room may not track. Always modify the original instance's fields directly instead of using `copy()` for Room-managed entities.
- **Kotlin `object` initialization order matters.** When converting a Java singleton to Kotlin `object`, the init block runs when the class is first loaded. If the init block references other objects that aren't initialized yet, you get a silent `null` or lateinit error. BellCollection's `init()` was renamed to `initialize()` and called explicitly after dependency injection.
- **Kotlin SAM conversion only works for single-method Java interfaces.** For multi-method interfaces (e.g., `OnSectionActionListener`), you must use `object : Interface { ... }`. Marking an interface as `fun interface` would break Java callers.
- **`@Volatile` is required on cross-thread fields in Kotlin.** Java fields accessed from multiple threads need `@Volatile` annotation in Kotlin (equivalent to Java's `volatile` keyword). Applied to: `Meditation.stopping`, `Meditation.paused`, `Audio.playing`.
- **Kotlin source sets must be explicitly declared in Kotlin DSL.** Unlike Groovy DSL which auto-detects `src/main/kotlin/`, the Kotlin DSL requires explicit `java.srcDir("src/main/kotlin")` in `sourceSets`. Same for test and androidTest.
- **ktlint 14.2.0 is latest (March 2026), detekt 1.23.8 is latest (Feb 2025).** 1.24.0 was speculated but hasn't been released. If detekt fails to resolve, check Gradle Plugin Portal for the current latest.
- **KSP with AGP 9.x requires `android.disallowKotlinSourceSets=false` in gradle.properties.** AGP 9.x disallows Kotlin source sets by default, but KSP registers its generated source directories via Kotlin source sets. Without this flag, the build fails with a source set configuration error.
- **`java.srcDirs()` (plural) is deprecated in AGP 9.x.** Use `java.srcDir()` (singular) per-directory when adding source directories to sourceSets. The plural form triggers deprecation warnings.
- **KSP version for Kotlin 2.2.x uses old format (`2.2.10-2.0.2`).** For Kotlin 2.3.x+, KSP uses simplified versioning (e.g., `2.3.7`). Match KSP version to the embedded Kotlin compiler version in AGP, not the kotlin-stdlib dependency version.
